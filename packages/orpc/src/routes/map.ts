import { buildRoute } from "./map/build-route"
import { getEditorState } from "./map/get-editor-state"
import { getMap } from "./map/get-map"
import { publishMap } from "./map/publish-map"
import { restoreMap } from "./map/restore-map"

export default {
	buildRoute,
	getEditorState,
	getMap,
	publishMap,
	restoreMap,
}
