{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "uni-app",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.16",
  "devDependencies": {
    "@biomejs/biome": "^2.0.0-beta.6",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
